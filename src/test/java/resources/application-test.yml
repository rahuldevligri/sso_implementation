spring:
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.H2Dialect
        show_sql: true
        hbm2ddl:
          auto: update
        jdbc:
          time_zone: UTC
          debug: true
  h2:
    console:
      enabled: true

postgres:
  datasource:
    primary-ds:
      url: jdbc:h2:mem:platform-SVC;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
      username: platform-SVC
      password: password
      driver-class-name: org.h2.Driver
      jpa:
        properties:
          hibernate:
            dialect: org.hibernate.dialect.H2Dialect
            show_sql: true
            hbm2ddl:
              auto: update
            jdbc:
              time_zone: UTC
              debug: true
      h2:
        console:
          enabled: true
auth:
  datasource:
    url: jdbc:h2:mem:platform-SVC;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
    username: platform-SVC
    password: password
    driver-class-name: org.h2.Driver
    jpa:
      properties:
        hibernate:
          dialect: org.hibernate.dialect.H2Dialect
          show_sql: true
          hbm2ddl:
            auto: update
          jdbc:
            time_zone: UTC
            debug: true
    h2:
      console:
        enabled: true
